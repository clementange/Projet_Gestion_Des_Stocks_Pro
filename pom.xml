<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/> <!-- lookup parent from repository -->
  </parent>
  <groupId>com.kfokam48</groupId>
  <artifactId>gestiondestock</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>API_Gestion_de_stock</name>
  <description>API de gestion de stock</description>

  <properties>
    <java.version>17</java.version>
    <lombok.version>1.18.30</lombok.version>
    <!-- Phase 0 : la version de Testcontainers geree par spring-boot-starter-parent (1.19.8)
         negocie une version d'API Docker (1.32) trop ancienne pour un daemon Docker recent
         (minimum accepte 1.40) -> negociation en echec. Version plus recente forcee ici pour que
         la negociation automatique aboutisse. -->
    <testcontainers.version>1.20.4</testcontainers.version>
    <!-- Phase 1 : Spring Modulith 1.2.x est la ligne alignee sur Spring Boot 3.3.x (voir la
         matrice de compatibilite du projet) ; ne pas monter en 1.3+/2.x sans monter Boot en
         meme temps. -->
    <spring-modulith.version>1.2.13</spring-modulith.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-bom</artifactId>
        <version>${spring-modulith.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>

    <!-- Phase 4c : remplace flickr4java (compte Flickr partage entre tous les tenants, cles en
         clair trouvees dans application.yml avant cet increment - voir docs/phase-4c-report.md)
         par un stockage objet compatible S3 auto-heberge. -->
    <dependency>
      <groupId>io.minio</groupId>
      <artifactId>minio</artifactId>
      <version>8.5.17</version>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>0.11.5</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>0.11.5</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>0.11.5</version>
      <scope>runtime</scope>
    </dependency>

    <!-- Connecteur PostgreSQL -->
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>${lombok.version}</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.6.0</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.vintage</groupId>
      <artifactId>junit-vintage-engine</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Phase 0 : filet de securite - Testcontainers PostgreSQL pour ne plus dependre d'un
         Postgres local demarre a la main pendant les tests d'integration. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Phase 4c : meme pattern que le conteneur PostgreSQL singleton ci-dessus, applique au
         nouveau module media (MinIO). -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>minio</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Phase 1 : rend les frontieres de module deja en place verifiables par un test
         (ApplicationModules.of(...).verify()), sans changement de comportement applicatif.
         spring-modulith-starter-core est ici en scope par defaut (compile), pas test : les
         annotations @NamedInterface posees sur des package-info.java de src/main/java (surface
         publique des modules, Phase 1 point 4) doivent etre resolues a la compilation du code de
         production, pas seulement des tests. spring-modulith-starter-test reste scope test (JUnit
         5 uniquement, pour ApplicationModules/Documenter). -->
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Phase 2 : traduit en tests qui cassent le build les regles d'architecture deja enoncees
         dans CLAUDE.md. Version alignee sur com.tngtech.archunit:archunit deja resolu
         transitivement (1.3.1, via spring-modulith-core) pour eviter tout conflit de version. -->
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>1.3.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- Phase 0 : le detecteur d'environnement Docker de Testcontainers embarque une copie
               figee (shadee) de docker-java-core dont le "ping" de compatibilite est envoye en
               API Docker 1.32 par defaut. Les daemons Docker recents ont retire le support des
               API < 1.40, ce qui fait echouer ce ping ("client version 1.32 is too old") avant
               meme la negociation de version normale du client docker-java principal. Forcer
               api.version a la plus haute valeur connue de cette copie figee (1.44) contourne le
               probleme ; un Docker daemon recent accepte sans souci une version de client egale
               ou inferieure a sa propre version d'API. -->
          <!-- ${argLine} reprend la valeur injectee par jacoco-maven-plugin (goal
               prepare-agent, -javaagent:...) : il ne faut pas l'ecraser, seulement lui
               ajouter le flag ci-dessus, sous peine de perdre la couverture JaCoCo. -->
          <argLine>@{argLine} -Dapi.version=1.44</argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>